
<h1><%= @cocktail.name %></h1>
<p><%= link_to "Add ingredients", new_cocktail_dose_path(@cocktail) %></p>
<ul>
  <% @cocktail.doses.each do |dose| %>
    <li><%= dose.quantity %>cl <%= dose.ingredient.name %><%= button_to "delete", dose_path(dose), method: :delete, class: 'btn btn-primary' %>
    </li>
  <% end %>

</ul>

<%= link_to 'Back to list', cocktails_path %>
